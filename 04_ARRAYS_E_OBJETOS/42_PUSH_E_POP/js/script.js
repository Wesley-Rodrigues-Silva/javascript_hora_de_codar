const array = ['a', 'b', 'c'];

array.push('d'); // adicionar um elemento ao final da array

console.log(array)

array.pop(); // remover o ultimo elemento do array

console.log(array)

const itemRemovido = array.pop(); // retorna o elemento que foi removido

console.log(itemRemovido);