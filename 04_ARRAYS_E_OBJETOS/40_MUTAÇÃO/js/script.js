const a = {
    name: 'Wesley'
}

const b = a;

console.log(a);
console.log(b);

/* Fazendo isso não estarei criando um objeto novo, ou seja caso eu adicione ou exclua qualquer dados das variaveis, tanto a const a, quando a cont b será mudada */