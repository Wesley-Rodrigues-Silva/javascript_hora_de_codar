const numbers = [5, 3, 4];

const otherNumbers = [1, 2, 3];

const allNumbers = numbers.concat(otherNumbers);

console.log(allNumbers);

const text = 'algum texto';

console.log(text.toUpperCase()); // deixar todas as letras maiuscula

console.log(typeof text.toUpperCase()) // retorna que é uma function

console.log(text.indexOf('g')) // localizar o indice da letra g
